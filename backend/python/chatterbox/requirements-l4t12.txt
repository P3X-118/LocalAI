--extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126/
# Pin torch/torchaudio to the newest matched pair the jetson-ai-lab jp6/cu126
# mirror actually serves (torchaudio caps at 2.10.0 there; torch/torchaudio
# minor versions must match). Left unpinned, pip prefers a newer PyPI
# cu130 aarch64 wheel that this host's CUDA 12.2 Jetson driver cannot run --
# torch.cuda.is_available() comes back False. See the CUDA toolchain mismatch
# note in the deployment repo's memory.
torch==2.10.0
torchaudio==2.10.0
# chatterbox imports pkg_resources, which setuptools>=81 removed. This
# line is only a documentary floor: the base venv already ships
# setuptools 65.5.0 (<81) so it is a no-op here, and runProtogen later
# force-upgrades setuptools past 81. The effective fix is the post-
# installRequirements `pip install 'setuptools<81'` in install.sh.
setuptools<81
transformers
numpy>=1.24.0,<1.26.0
chatterbox-tts@git+https://git@github.com/mudler/chatterbox.git@faster
accelerate
